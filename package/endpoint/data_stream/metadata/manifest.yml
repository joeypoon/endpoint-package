title: Endpoint Metadata
type: metrics
elasticsearch:
  index_template:
    mappings:
      dynamic: false
    settings:
      index:
        sort.field:
          - agent.id
          - "@timestamp"
        sort.order:
          - asc
          - desc
